<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dazuoye.mapper.UserMapper">

    <select id="loginStudent" resultType="User">
        SELECT id, username, password, 'student' AS role, real_name, class_name
        FROM students
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="loginTeacher" resultType="User">
        SELECT id, username, password, 'teacher' AS role, real_name, NULL AS class_name
        FROM teachers
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="findAllStudents" resultType="User">
        SELECT id, username, password, 'student' AS role, real_name, class_name
        FROM students ORDER BY id
    </select>

    <select id="countStudents" resultType="int">
        SELECT COUNT(*) FROM students
    </select>

</mapper>
